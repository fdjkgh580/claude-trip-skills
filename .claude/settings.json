{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -n \"$(git status --porcelain 2>/dev/null)\" ]; then echo '⚠️ 偵測到未儲存的變更。請在你的回應中**逐字**附上以下提醒（不要改寫、不要省略任何一種觸發方式）：「💾 目前還有變更尚未儲存，跟我說「儲存」「save」「備份」其中一個，或打 /backup，就會把變更存到雲端。不然關掉視窗會不見。」用使用者的語言（預設繁體中文）。'; fi"
          }
        ]
      }
    ]
  }
}
